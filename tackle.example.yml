# save this file to tackle.yml
project1:
  root: /var/www/project1
  setup:
    - docker-compose run web bundle install
    - docker-compose run web db:create

project2:
  root: /var/www/project2
  active: false